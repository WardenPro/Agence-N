<form method="post" action="{{ path('app_liste_frais_delete', {'id': liste_frai.id, 'noteId': noteId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ liste_frai.id) }}">
    <button class="btn btn-danger button">Delete</button>
</form>
